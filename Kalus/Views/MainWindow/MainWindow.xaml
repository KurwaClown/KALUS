<Window x:Class="Kalus.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kalus"
        xmlns:panel="clr-namespace:Kalus.Controls.ControlPanel"
        xmlns:console="clr-namespace:Kalus.Controls.Console"
        xmlns:tabs="clr-namespace:Kalus.Controls.Tabs"
        mc:Ignorable="d"
        Title="Kalus" Height="640" Width="800"
        Loaded="Window_Loaded"
        Closing="Window_Closing" MinWidth="720" MinHeight="440"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="150" />
        </Grid.RowDefinitions>
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem x:Name="checkInterval" Header="Checks Interval">
                    <MenuItem Header="300" Click="SetCheckInterval" />
                    <MenuItem Header="500" Click="SetCheckInterval" />
                    <MenuItem Header="1000" Click="SetCheckInterval" />
                    <MenuItem Header="3000" Click="SetCheckInterval" />
                    <MenuItem Header="5000" Click="SetCheckInterval" />
                </MenuItem>
            </ContextMenu>
        </Grid.ContextMenu>
        <TabControl Margin="5,5,5,5">
            <TabItem Header="Characters">
                <tabs:Characters x:Name="charactersTab"></tabs:Characters>

            </TabItem>

            <TabItem Header="Preferences">
                <tabs:Preferences x:Name="preferencesTab"/>
            </TabItem>
            <TabItem Header="Console">
                <console:ConsoleView x:Name="consoleTab" x:FieldModifier="public"></console:ConsoleView>
            </TabItem>
        </TabControl>
        <panel:ControlPanel x:Name="controlPanel" Grid.Row="1" Grid.Column="1" Grid.RowSpan="1" Margin="5,0,5,5"></panel:ControlPanel>
        
    </Grid>
</Window>